import { PopupComponent } from './popup.component';

describe('PopupComponent', () => {
  let component: PopupComponent;

  beforeEach(() => {
    // component = new PopupComponent(null);
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
