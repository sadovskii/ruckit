import { AboutExtensionComponent } from './about-extension.component';

describe('AboutExtensionComponent', () => {
  let component: AboutExtensionComponent;

  beforeEach(() => {
    component = new AboutExtensionComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
