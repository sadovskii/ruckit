import { BlackListComponent } from './black-list.component';

describe('BlackListComponent', () => {
  let component: BlackListComponent;

  beforeEach(() => {
    component = new BlackListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
