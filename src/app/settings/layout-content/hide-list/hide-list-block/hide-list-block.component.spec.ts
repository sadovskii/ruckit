import { HideListBlockComponent } from './hide-list-block.component';

describe('HideListBlockComponent', () => {
  let component: HideListBlockComponent;

  beforeEach(() => {
    component = new HideListBlockComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
