import { HideListComponent } from './hide-list.component';

describe('HideListComponent', () => {
  let component: HideListComponent;

  beforeEach(() => {
    component = new HideListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
