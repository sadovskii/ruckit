import { SetNewPasswordComponent } from './set-new-password.component';

describe('SetNewPasswordComponent', () => {
  let component: SetNewPasswordComponent;

  beforeEach(() => {
    component = new SetNewPasswordComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
