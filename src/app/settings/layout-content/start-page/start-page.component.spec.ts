import { StartPageComponent } from './start-page.component';

describe('StartPageComponent', () => {
  let component: StartPageComponent;

  beforeEach(() => {
    component = new StartPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
