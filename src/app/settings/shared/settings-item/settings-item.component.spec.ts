import { SettingsItemComponent } from './settings-item.component';

describe('SettingsItemComponent', () => {
  let component: SettingsItemComponent;

  beforeEach(() => {
    component = new SettingsItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
