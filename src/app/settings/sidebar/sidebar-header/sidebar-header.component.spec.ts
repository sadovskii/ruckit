import { SidebarHeaderComponent as SidebarHeaderComponent } from './sidebar-header.component';

describe('HeaderComponent', () => {
  let component: SidebarHeaderComponent;

  beforeEach(() => {
    component = new SidebarHeaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
