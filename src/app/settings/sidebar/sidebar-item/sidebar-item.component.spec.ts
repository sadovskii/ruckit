import { SidebarItemComponent } from './sidebar-item.component';

describe('SidebarItemComponent', () => {
  let component: SidebarItemComponent;

  beforeEach(() => {
    component = new SidebarItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
