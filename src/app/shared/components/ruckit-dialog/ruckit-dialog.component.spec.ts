import { RuckitDialogComponent } from './ruckit-dialog.component';

describe('RuckitDialogComponent', () => {
  let component: RuckitDialogComponent;

  beforeEach(() => {
    component = new RuckitDialogComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
