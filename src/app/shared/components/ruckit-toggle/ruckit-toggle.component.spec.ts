import { RuckitToggleComponent } from './ruckit-toggle.component';

describe('RuckitToggleComponent', () => {
  let component: RuckitToggleComponent;

  beforeEach(() => {
    component = new RuckitToggleComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
