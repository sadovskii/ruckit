import { RestrictedButtonDirective } from './restricted-button.directive';

describe('RestrictedButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedButtonDirective();
    expect(directive).toBeTruthy();
  });
});
