import { RestrictedToggleDirective } from './restricted-toggle.directive';

describe('RestrictedToggleDirective', () => {
  it('should create an instance', () => {
    // const directive = new RestrictedToggleDirective();
    // expect(directive).toBeTruthy();
  });
});
