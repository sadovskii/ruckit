export enum NebularThemes {
    default = "default",
    dark =  "dark",
    corporate = "corporate",
    cosmic = "cosmic"
}

export enum ViewVersions {
    small = 1,
    medium,
    large
}

export enum OpenFullPageAction {
    Home = "home",
    BlackList = "blacklist",
    Hidelist = "hidelist",
    Settings = "settings"
}